#!/usr/bin/env node

var path = require('path'),
    fs   = require('fs'),
    lib  = path.join(path.dirname(fs.realpathSync(__filename)), '../lib'),
    cmd  = new (require(lib + '/Command').Command);

cmd.run();
